-- Conky, a system monitor https://github.com/brndnmtthws/conky
--
-- This configuration file is Lua code. You can write code in here, and it will
-- execute when Conky loads. You can use it to generate your own advanced
-- configurations.
--
-- Try this (remove the `--`):
--
--   print("Loading Conky config")
--
-- For more on Lua, see:
-- https://www.lua.org/pil/contents.html

conky.config = {
    alignment = 'top_right',
    background = false,
    border_width = 2,
    border_outer_margin = 10,
    cpu_avg_samples = 2,
    default_color = '#e5e9f0',
    default_outline_color = '#81a1c1',
    default_shade_color = '#81a1c1',
    double_buffer = true,
    draw_borders = false,
    draw_graph_borders = true,
    draw_outline = false,
    draw_shades = false,
    extra_newline = false,
    font = 'Terminus:size=12',
    gap_x = 30,
    gap_y = 73,
    minimum_height = 5,
    minimum_width = 5,
    net_avg_samples = 2,
    no_buffers = true,
    out_to_console = false,
    out_to_ncurses = false,
    out_to_stderr = false,
    out_to_x = true,
    own_window = true,
    own_window_class = 'Conky',
    own_window_colour = '#0f111a', 
    own_window_type = 'override',
    show_graph_range = false,
    show_graph_scale = false,
    stippled_borders = 0,
    update_interval = 1.0,
    uppercase = false,
    use_spacer = 'none',
    use_xft = true,
}

conky.text = [[
${color #ebcb8b}$color ${scroll 32 Arch Linux - $sysname $nodename $kernel $machine}
$hr
${color #ebcb8b}up$color $uptime
${color #81a1c1}freq (GHz):$color $freq_g
${color #ff4151}ram$color $mem/$memmax - $memperc% ${membar 4}
${color #a3bebc}swap$color $swap/$swapmax - $swapperc% ${swapbar 4}
${color #81a1c1}cpu$color $cpu% ${cpubar 4}
${color #ebcb8b}processes$color $processes  ${color grey}running$color $running_processes
$hr
${color grey}storage
 ${color #ff4151}/ $color${fs_used /}/${fs_size /} ${fs_bar 6 /}
 ${color #a3bebc}/home $color${fs_used /home/}/${fs_size /home/} ${fs_bar 6 /home/}
 ${color #81a1c1}HDD $color${fs_used /mnt/HDD/}/${fs_size /mnt/HDD/} ${fs_bar 6 /mnt/HDD/}
${color grey}network
up$color ${upspeed} ${color grey} - down$color ${downspeed}
$hr
${color grey}name              PID     CPU   MEM
${color lightgrey} ${top name 1} ${top pid 1} ${top cpu 1} ${top mem 1}
${color lightgrey} ${top name 2} ${top pid 2} ${top cpu 2} ${top mem 2}
${color lightgrey} ${top name 3} ${top pid 3} ${top cpu 3} ${top mem 3}
${color lightgrey} ${top name 4} ${top pid 4} ${top cpu 4} ${top mem 4}
${color lightgrey} ${top name 5} ${top pid 4} ${top cpu 4} ${top mem 4}
${color lightgrey} ${top name 6} ${top pid 4} ${top cpu 4} ${top mem 4}
]]
